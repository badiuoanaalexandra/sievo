export const sortingOrder = (state) => state.filteredProjectsReducer.sortingOrder;

export const description = (state) => state.filteredProjectsReducer.description;

export const filteredProjects = (state) => state.filteredProjectsReducer.filteredProjects;
